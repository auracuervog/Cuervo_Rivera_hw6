#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char **argv){
  double ek;
  double alpha;
  double masa_proton, q_proton;

  ek = atof(argv[1]);
  alpha = atof(argv[2]);
  masa_proton = pow(1.672621777*10,-27);
}
